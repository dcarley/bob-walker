bob-walker bootloader
---------------------

This will build a 512byte binary for use as a bootloader.

Build by running `make`

You can run this by mounting the outputted image as a floppy disk in VirtualBox.

